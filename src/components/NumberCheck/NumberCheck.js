import './NumberCheck.css';

const NumberCheck = () => {
    return (
        <div className="number-check__button">
            <button type="submit">Check for duplicates!</button>
        </div>
    );
}

export default NumberCheck;